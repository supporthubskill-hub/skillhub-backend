name: Add admin improvements batch

on:
  push:
    branches:
      - chatgpt/admin-improvements-batch

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt/admin-improvements-batch
      - name: Patch backend
        run: python scripts/add_admin_batch.py
      - name: Validate syntax
        run: node --check server.js
      - name: Commit
        run: |
          git config user.name "SkillHub Automation"
          git config user.email "actions@users.noreply.github.com"
          git add server.js
          git commit -m "Add admin restore, reasons and user details"
          git push origin HEAD:chatgpt/admin-improvements-batch
